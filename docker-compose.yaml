version: '3'

services: 
  app1:
    container_name: api01
    build: ./hello-world-api
    ports:
      - "3000:3000"
    environment:
      - MESSAGE=yessi

    
  app2:
    container_name: api02
    build: ./hello-world-api
    ports:
      - "3001:3000"
    environment:
      - MESSAGE=yessi2
    
  app3:
    container_name: api03
    build: ./hello-world-api
    ports:
      - "3002:3000"
    environment:
      - MESSAGE=yessi3
  
